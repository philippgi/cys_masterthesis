backend = "redis";
servers = "redis:6379";
new_schema = true;
autolearn = false;
min_tokens = 11;

statfile {
  symbol = "BAYES_HAM";
  spam = false;
}

statfile {
  symbol = "BAYES_SPAM";
  spam = true;
}

learn_condition = <<EOD
return function(task, is_spam, is_unlearn)
  return true
end
EOD;
