# RS3: Rules + Neural (symbol-based, manual training only)

enabled = true;
servers = "redis:6379";
timeout = 20s;

rules {
  default {
    train {
      max_trains = 1000;
      max_usages = 20;
      learning_rate = 0.01;
      max_iterations = 25;

      autotrain = false;
      learn_mode = "balanced";
      classes_bias = 0.0;
    }

    allow_local = true;

    symbol_spam = "NEURAL_SPAM";
    symbol_ham = "NEURAL_HAM";

    ann_expire = 8640000;
  }
}
